export enum Period {
  CUSTOM = 'custom',
  MONTHS = 'months',
  WEEK = 'week',
  YEAR = 'year',
  DAY = 'day',
}

export enum CheckoutStatus {
  IN_PROGRESS = 'in-progress',
  PLANNED = 'planned',
  COMPLETED = 'completed',
  SIGNED = 'signed',
  ARCHIVE = 'archive',
}
